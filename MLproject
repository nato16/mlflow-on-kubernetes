name: My Project

docker_env:
    image:  docker.io/natihd16/mlflow-project-null:latest
    volumes: ["./data:/home"]

entry_points:
  main:
    parameters:
      input_file: path
    command: "python train.py {input_file}"
