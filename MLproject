name: project-null

docker_env:
    image: natihd16/mlflow-project-null:latest
    volumes: ["${HOME}/data:/home"]

entry_points:
  main:
    parameters:
      input_file: string
    command: "python train.py {input_file}"
