name: project-null

docker_env:
    image: project-null
    volumes: ["${HOME}/data:/home"]

entry_points:
  main:
    parameters:
      input_file: string
    command: "python /mlflow/projects/code/train.py {input_file}"
